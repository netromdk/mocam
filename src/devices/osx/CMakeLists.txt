FIND_LIBRARY(FOUNDATION_LIB Foundation)
FIND_LIBRARY(AV_LIB AVFoundation)

MARK_AS_ADVANCED(
  FOUNDATION_LIB 
  AV_LIB
  )

SET(
  OSX_LIBS 
  ${FOUNDATION_LIB} 
  ${AV_LIB}
  )

MESSAGE("OSX LIBS: ${OSX_LIBS}")

ADD_DEFINITIONS(
  -DMAC
  -DGCC_VISIBILITY
  )

ADD_SUBDIRECTORY(isight)